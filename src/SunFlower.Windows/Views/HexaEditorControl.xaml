<UserControl x:Class="SunFlower.Windows.Views.HexaEditorControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:SunFlower.Windows.ViewModels"
             xmlns:wpfHexaEditor="clr-namespace:WpfHexaEditor;assembly=WPFHexaEditor"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">
    <UserControl.DataContext>
        <local:HexViewModel/>
    </UserControl.DataContext>
    <wpfHexaEditor:HexEditor
        FontFamily="Consolas"
        FontSize="16"
        Background="{DynamicResource BorderBrush}"
        Foreground="{DynamicResource PrimaryTextBrush}"
        ReadOnlyMode="True"
        Stream="{Binding Reader.Stream}"/>
</UserControl>
